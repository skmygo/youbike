# 隔夜衝刺總結（2026-08-06 22:35 → 08-07 08:00）

**線上**：<https://youbike.itsmygo.uk>　·　**簡報**：<https://youbike.itsmygo.uk/slides.html>（← → 翻頁）
**排程後台**：<https://youbike-dagster.itsmygo.uk>（CF Access）　·　**原始碼**：`skmygo/youbike`

一句話：**把「事後才知道站空了」變成「兩小時前就知道，而且知道車該從哪裡搬過來」。**
整晚 36 個 commit，主線 M0–M6 全部完成上線，加分梯隊 4 項完成、1 項驗證後判定不值得做。

---

## 早上直接這樣 demo（3 分鐘）

1. **首頁** — 此刻全市誰在壞（地圖＋KPI），底部脈搏帶把今天疊在同星期幾的歷史常態上
2. **左欄「未來一小時」** — 模型說幾站可能無車，這是現況面板給不出的資訊
3. **`/dispatch` 調度建議** — 直接給任務單「這站缺 11 台，從 1.5 km 外那站收 3 台」；
   配不到鄰近滿位站的明白標示「需由調度中心出車」
4. **`/alerts` 切「預測」** — 同一批站，規則型要等它空了才亮燈
5. **`/model`** — 好與壞一次講完（回測、KPI、限制）
6. **`/replay`** — 拉到 6 月某個早上八點，看尖峰怎麼擴散

> 07:23 實測：早尖峰進行中，60 分內可能無車 241 站（06:37 還是 174 站）、規則型警示 216 筆。
> **預測比規則型先反應**，這在現場直接看得到。

---

## 做完了什麼

| 里程碑 | 內容 |
|---|---|
| M0–M1 | monorepo、多階段 Dockerfile、Dokploy 部署、歷史 6 個月 → parquet → S3 → 容器引導、9 支核心 API |
| M2 | Dagster 每 10 分鐘：爬取 → 服務層 → 警示物化，全自動 |
| M3 | 前端五頁（即時指揮／回放／警示／區域分析／關於），signature 是全市空滿脈搏帶 |
| M4 | 41 個特徵、12 個 LightGBM、兩個 baseline、MLflow、6 月回測 report.json |
| M5 | 線上推論（特徵與訓練逐欄對齊）、`forecast_every_30min`、6 支預測/調度 API、調度頁與預測帶 |
| M6 | `/model` 回測報告頁、KPI1 調度改善模擬、README、簡報大綱 |
| S1 | 主動通知：新升級的警戒/嚴重站推 webhook，警示頁通知紀錄 |
| S2 | 預測不確定帶：8 個分位數模型，站點抽屜畫成真的帶 |
| S3 | **天氣特徵：驗證後放棄**（見下） |
| S4 | 可放映簡報 `/slides.html`（11 頁） |
| S5 | demo GIF 進 README |

## 關鍵數字（6 月測試集，訓練與驗證全程沒碰過）

| 指標 | 值 | 對照 |
|---|---|---|
| 60 分預測平均誤差 | **2.05 台** | 現況延續 2.21（+7.3%） |
| 180 分預測平均誤差 | **3.15 台** | 現況延續 3.99（**+21.0%**） |
| 空車事件偵測率 | **68.1%**（35,450 起） | 規則型 0%（事發才知道） |
| 平均提前預警 | **147 分鐘** | 規則型 0 分鐘 |
| KPI1 調度改善 | 每槽 40 站時 **32.1%** 無車時間可消除 | 純規則型 27.8%（+4.3pp） |
| 預測區間覆蓋率 | 76–78% | 目標 80% |

**最有價值的發現（不在命題要求裡，是資料自己講出來的）**：
無車時間高度集中——最常空的 **76 個站（全體 5%）佔掉 20.5% 的無車時間**，其中 **13 個站超過一半時間沒車**。
這類站是車輛配置與站點規模的問題，調度只能緩解不能根治。
**建議：這 13 站該加車柱或提高基準配車量，不該再叫調度車去追。**

---

## 半夜踩的坑（都修好了，記錄下來免得重踩）

1. **00:45 訓練把 production 主機吃掛** — 沒設資源上限，swap 灌滿、同機 Dokploy 重啟。
   對策：所有重活戴 `systemd-run` 資源罩（4G／400%），**被 OOM 殺就縮規模不調上限**，
   抽樣 35%→22%，峰值壓到 1.88G。此後整晚沒再出事。
2. **DuckDB `.df()` 6.7 秒衝破 4G** — 改成 SQL 端 CAST 成 4 bytes ＋ Arrow `self_destruct` 交棒 pandas。
   先寫 memcheck smoke test 驗過才開跑，沒有再賭一次。
3. **`_out/models/` 是 root 擁有**（先前 docker 掛載留下的）→ LightGBM 存檔被拒，exit 0 但沒模型。
4. **事件偵測率 6.5% 是假象** — 那是用 70% 門檻算的，而最佳門檻在 0.10–0.30。
   改成雙門檻並存後才是 68.1%。系統線上也雙軌輸出。
5. **entrypoint 在歷史資料已存在時整段跳過 bootstrap** — 新訓練的模型永遠進不了容器。
   這個不修，整晚的訓練等於白做。
6. **調度配對出現 19 km 的荒謬任務** — 加 3 公里上限，配不到就標「調度中心出車」。
7. **KPI 模擬第一版把預防與補救混在一個排序** — 已空站機率恆 0.99+ 會吃光預防名額，
   模型只贏 1–3pp。拆成兩個佇列後結論才成立。

## 誠實的限制

- **誤報率 52%**：營運門檻照 F1 最佳選的代價。所以調度頁按風險排序取前 N，不是把警報全丟出來。
- **滿位事件較難預測**（偵測率 51%）：事件本身少，3,909 起 vs 35,450 起。
- **沒有天氣**：本機沒有中央氣象署 API key，而且就算爬到 CODiS 歷史觀測，
  **線上推論時仍拿不到即時值與預報**——做出來是永遠 degraded 的特徵，所以誠實地不做。
- **官方行事曆驗證後不採用**：官方 11 個平日假日資料驅動全抓到，另外多抓 7 天（連假前後請假潮），
  換過去反而丟資訊；2026 全年也沒有補班日。佐證檔在 `ref_data/`。
- **線上預測正在暖機**：即時快照從昨晚才開始累積，不足 7 天的 lag 用「同站同星期幾同時段的
  上一次實際觀測」橋接。即時槽佔比整晚從 0.3% 爬到 **4.6%**，介面上誠實顯示這個數字。

## 系統現況（無人自主運作中）

- Dagster 每 10 分鐘爬取 → 物化 → 重算警示 → 推播新升級的站；每 30 分鐘跑一次推論
- 整晚守夜健檢 6 次全綠，公開端點 9 支 200、回應 0.33–0.47 秒
- 20 個模型逐一進 MLflow（experiment `youbike-hackathon`，共 30 runs）
- 模型與回測報告版本化在 S3 `models/`，容器啟動自動拉最新

## 還沒做的（如果還有時間）

- 天氣與活動行事曆（需要 API key）
- 把調度任務單接進實際派工系統
- 那 13 個結構性缺車站的配置重算（這件事的效益可能比模型調參大得多）
